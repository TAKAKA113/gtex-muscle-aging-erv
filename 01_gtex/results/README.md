# Selected GTEx results

This directory contains selected, compact outputs from the GTEx analysis:

- `figures/` — PDF and PNG figures
- `tables/` — CSV summary tables

Large intermediate objects and full expression matrices will not be included.
